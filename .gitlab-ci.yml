
stages:
  - build
  - test

unit-test-job:   # This job runs in the test stage.
  stage: test    # It only starts when the job in the build stage completes successfully.
  script:
    - echo "Running unit tests... This will take about 60 seconds."
    - sleep 60
    - echo "Code coverage is 90%"

build_img:
  image: docker
  stage: build
  services:
    - docker:dind
  script:
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker build --cache-from $CI_REGISTRY_IMAGE/api:latest -t $CI_REGISTRY_IMAGE/api:$CI_COMMIT_SHA /devops4115904/doodlestudent/api
    - docker push $CI_REGISTRY_IMAGE/api:$CI_COMMIT_SHA
    - docker build --cache-from $CI_REGISTRY_IMAGE/front:latest -t $CI_REGISTRY_IMAGE/front:$CI_COMMIT_SHA /devops4115904/doodlestudent/front
    - docker push $CI_REGISTRY_IMAGE/front:$CI_COMMIT_SHA


